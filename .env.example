# Base URL of the backend API (Express server). The frontend only talks to the backend
# through this URL. Change it when the backend runs on another host or port.
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Currency symbol shown in the UI
NEXT_PUBLIC_CURRENCY=Rs
